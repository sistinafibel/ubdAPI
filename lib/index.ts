const ubd = (data: number) => (data / 172212).toFixed(3);
const ubd2 = (data: number) => (data / 30000000000).toFixed(3);
const ubds = (data: number) => (data / 170000).toFixed(3);

export { ubd, ubd2, ubds };
